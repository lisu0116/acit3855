from sqlalchemy import create_engine, text
e = create_engine("mysql+pymysql://lisu:000423@127.0.0.1:3307/acit3855?charset=utf8mb4", future=True, echo=True)
with e.connect() as c:
    print("user:", c.execute(text("SELECT CURRENT_USER()")).scalar_one())
    print("db:", c.execute(text("SELECT DATABASE()")).scalar_one())
